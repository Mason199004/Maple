namespace MapleCore.Core
{
	public class Main
	{
		// starting point for maple build, call init, process command, execute command 
		public static void Start(string[] args)
		{
			//eventually check for existing Maple daemon, not implemented yet, call init
			Init();
		}

		private static void Init()
		{
			
		}
	}
}